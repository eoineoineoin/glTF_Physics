{
    "$schema": "http://json-schema.org/draft-04/schema",
    "title": "KHR_collision_shapes glTF extension",
    "type": "object",
    "description": "Parameters describing a mesh collider.",
    "allOf": [ { "$ref": "glTFProperty.schema.json" } ],
    "properties": {
        "mesh": {
            "description": "The ID of the mesh from which to build the collision representation.",
            "allOf": [{"$ref": "glTFid.schema.json"}]
        },
        "extensions": { },
        "extras": { }
    },
    "required": [
        "mesh"
    ]
}
